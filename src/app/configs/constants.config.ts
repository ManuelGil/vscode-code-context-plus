/**
 * Extension constants.
 */

/** Extension identifier (commands and configuration prefix). */
export const EXTENSION_ID = 'codeContextPlus';

/** Marketplace / package extension name (publisher.name). */
export const EXTENSION_NAME = 'vscode-code-context-plus';

/** Display name. */
export const EXTENSION_DISPLAY_NAME = 'CodeContext+';

/** Marketplace publisher. */
export const USER_PUBLISHER = 'imgildev';

/** GitHub user. */
export const USER_NAME = 'ManuelGil';

/** Repository URL. */
export const REPOSITORY_URL = `https://github.com/${USER_NAME}/${EXTENSION_NAME}`;

/** Default enable state. */
export const DEFAULT_ENABLE_SETTING = true;

/** Default notes folder. */
export const DEFAULT_NOTES_FOLDER = '.context/notes';
